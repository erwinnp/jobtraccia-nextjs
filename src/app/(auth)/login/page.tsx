import LoginForm from '@/features/user/components/login-form';

const LoginPage = () => {
  return (
    <section className='w-full max-w-screen-xl mx-auto min-h-screen flex items-center justify-center'>
      <LoginForm />
    </section>
  );
};

export default LoginPage;
